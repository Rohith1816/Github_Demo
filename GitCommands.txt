
install git bash

link it with your github account with this commands :: 

    $ git config --global user.name "USER NAME"
    $ git config --global user.email "MAIL ID"

Download github extension in your visual codes (GitHub.vscode-pull-request-github);

login with your GitHub account in your vs codes profile.

after your login it will ask us to authenticate just follow the instructions provided by it.

Sucessfully Connected with your account!!



git clone "https link"(without quotes)

change the directory to the Repo

make necessary changes 

git status  - to check the status

git add (file name) or use period (.) for all files

git commit -m "Commit message" -m "Commit description" 

git push origin (branch name) usually main or master depends on the system

git branch - shows the available branches in the Repo.

git checkout -b Branch Name(Issue Number).

git diff branch_Name :- shows the changes that i have made in other branches ...